# G0 実機スパイク — 手順と結果

> 計画: [docs/implementation-plan.md](implementation-plan.md) の G0
> 設計: [docs/watch-alarm-design.md](watch-alarm-design.md)
> 作成日: 2026-08-27

**使い捨てのコードです。** 計測が終わったら `apple-watch/.../Spike/` と
`spike/peripheral-sim/` を削除し、`TuretetteWatchApp.swift` と `AppDelegate.swift` の
スパイク分岐（各 1 ブロック）を消してください。

---

## 0. 何を確かめるのか

後続フェーズの設計を左右する 3 つの未知。**外れると P2 / P3 を作り直すことになります。**

| # | 問い | 外れたときの影響 |
|---|---|---|
| G0-1 | 背景 BLE 起床（`bluetooth-central` + notify）が実際に来るか | 来なければ方式1 が丸ごと成立しない |
| G0-2 | 5 回/24h の予算が `didDisconnect` 起床も数えるか（設計 §10-2） | 数えるなら §6 の予算管理を大幅に厳しくする |
| G0-3 | 背景起床 1 回の実行枠で歩数・活動の遡り問い合わせが返るか（§14.4） | 返らなければ P3 の遡及検出は前面専用に格下げ |

---

## 1. 必要なもの

| | 理由 |
|---|---|
| **Apple Watch Series 6 以降 / watchOS 9 以降の実機** | 背景 BLE 起床の動作要件（§2.1）。**シミュレータでは再現しません** |
| Mac（ペリフェラル役） | iPhone アプリはまだ無いので Mac が相手役を務めます |
| 数時間〜1 日の放置時間 | 24 時間のローリングウィンドウを見るため |

> iPhone ではなく Mac を相手役にしていますが、G0 で確かめたいのは **Watch 側の挙動**なので
> これで成立します。iPhone 固有の背景アドバタイズの挙動は P4 で別途確認します。

---

## 2. 準備

### 2-1. Mac 側（ペリフェラル役）

```bash
cd spike/peripheral-sim && swift run PeripheralSim
```

初回は Bluetooth の許可を求められます。拒否された場合は
システム設定 → プライバシーとセキュリティ → Bluetooth でターミナルを許可してください。

対話コマンド:

| キー | 動作 |
|---|---|
| `n` | notify を即時 1 回送る |
| `d` | 接続を切る（フェーズ B 用） |
| `i` | 送信間隔を変更する |
| `s` | 送信状況（送信数・連番）を表示 |
| `q` | 終了 |

### 2-2. Watch 側

`Spike/SpikeLog.swift` の `SpikeConfig.enabled` が `true` であることを確認し、実機へインストールします。
起動すると本体ではなく計測画面が出ます。画面は 3 タブ（状態 / 集計 / ログ）。

初回起動時に **モーションと Bluetooth の許可**を求められるので、両方許可してください。
`CMMotionActivityManager.authorizationStatus()` が「許可」になっていることを
状態タブ下部で確認します。ここが「拒否」だと G0-3 が測れません。

---

## 3. 計測

### フェーズ A — notify だけで予算を使い切る（G0-1 と G0-2 の前半）

1. Watch でアプリを開き、状態タブで **フェーズ A** を選ぶ
2. Mac 側が「購読されました」と出るまで待つ（接続と notify 購読の完了）
3. Mac 側で `i` → `600`（10 分間隔）に設定する
4. **Watch のアプリを閉じ、手首から外して放置する**
   - アプリを開くと予算がリセットされるので、計測中は開かないこと
5. 2〜3 時間放置する
6. Watch でアプリを開き、集計タブを読む

**読み取り方**

| 見るところ | 意味 |
|---|---|
| 背景で受けた notify の件数 | **0 なら G0-1 は不成立。** 背景起床が来ていない |
| ログの `notify` 行の「取りこぼし=N」 | 送られたが届かなかった数。Mac 側 `s` の送信数と突き合わせる |
| `budgetNear` / `budgetExceeded` の記録 | 何件目で予算が尽きたか。**5 件前後で止まるはず** |
| `restore` の件数 | OS がアプリを起こし直した回数。0 でなければ状態復元も効いている |

### フェーズ B — 切断だけで予算を使い切る（G0-2 の本題）

1. Watch でアプリを**開く**（これで予算がリセットされる）
2. 状態タブで **フェーズ B** を選ぶ
3. Mac 側で `i` → `3600`（1 時間間隔。notify で予算を消費させないため）
4. Watch のアプリを閉じ、放置する
5. **Mac 側で `d` を 10 分おきに 8 回ほど押す**（切断 → 自動再接続を繰り返させる）
6. Watch でアプリを開き、集計タブを読む

**読み取り方**

| 結果 | 結論 |
|---|---|
| 背景での切断イベントが 5 件前後で止まり、`budgetExceeded` が出る | **切断起床も予算を消費する。** §6 の予算管理を厳しくする必要がある |
| 8 回すべて記録され、`budgetExceeded` が出ない | 予算は notify 起床だけを数えている。設計どおり |

### G0-3 — 遡り問い合わせは実行枠に収まるか

フェーズ A / B の計測中に自動で測られます（起床のたびに 1 回）。
**歩数データが必要なので、計測中は Watch を装着して普通に歩いてください。**
手首から外して放置すると歩数が 0 になり、所要時間だけしか分かりません。

**読み取り方**

| 見るところ | 意味 |
|---|---|
| 開始 / 完了 / 未返却 の数 | **未返却が多ければ、背景実行枠で返っていない** |
| 所要 中央値 / 最大 | §14.4 の仮定（GPS と違って枠内に返る）が正しいかの数字 |
| ログの `probeLost` 行 | 打ち切りか、次の起床まで返らなかったかが分かる |

---

## 4. 結果の記録

計測が終わったら、**このファイルの下の表を埋めて**ください。
実装計画の G1 と、設計文書 §10 の未検証事項がここで解消されます。

### G0-1 背景 BLE 起床

| 項目 | 実測値 | 備考 |
|---|---|---|
| 計測日時 | | |
| 機種 / watchOS | | |
| 背景で受けた notify | | |
| 取りこぼし | | |
| 状態復元の回数 | | |
| **結論** | | 成立 / 不成立 |

### G0-2 予算の数え方

| 項目 | フェーズ A（notify） | フェーズ B（切断） |
|---|---|---|
| 背景で起きた回数 | | |
| `budgetNear` が出た件数目 | | |
| `budgetExceeded` が出た件数目 | | |
| **結論** | | 切断は予算を消費する / しない |

### G0-3 遡り問い合わせ

| 項目 | 実測値 |
|---|---|
| 開始 / 完了 / 未返却 | |
| 所要 中央値 | |
| 所要 最大 | |
| **結論** | 枠内に返る / 返らない |

### 設計への反映

| 反映先 | 内容 |
|---|---|
| §6 予算管理 | |
| §13.6 背景更新の間隔 | |
| §14.4 遡り問い合わせ | |
| P2 / P3 の設計変更 | |

---

## 5. 後始末

```bash
git rm -r apple-watch/TuretetteWatch/TuretetteWatch/Spike spike/peripheral-sim
```

加えて、次の 2 か所を手で戻します（どちらもコメント付きの 1 ブロックです）。

- `TuretetteWatchApp.swift` — `SpikeConfig.enabled` の分岐
- `AppDelegate.swift` — `SpikeRunner.shared.handle(task)` の行

Xcode プロジェクトからも `Spike` グループを削除してください。

---

## 6. 計測時の注意

- **アプリを開くと予算がリセットされます**（WWDC22）。計測中は開かないこと
- 24 時間のローリングウィンドウなので、フェーズ A と B は日をまたいで分けるのが確実
- Watch の「アプリのバックグラウンド更新」がオンになっていることを確認する
- 集計は直近 24 時間ぶんだけを対象にしています。それより古い記録はログタブで見てください
- Mac をスリープさせると広告が止まります。電源接続 + スリープ抑止（`caffeinate -s`）を推奨
