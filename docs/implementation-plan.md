# Turetette 実装計画

> 出典: [docs/watch-alarm-design.md](watch-alarm-design.md)(§番号はすべてこの文書を指す)
> 進捗の可視化: [docs/roadmap.html](roadmap.html)
> 作成日: 2026-08-26

---

## 0. 方針

設計検討(§0〜§16)から導かれた、実装順序を決める 5 つの判断。

| # | 判断 | 根拠 |
|---|---|---|
| 1 | **発報も検知も Watch 側に置く。** iPhone は「ペリフェラル役」と「位置の事前送信役」 | §1 |
| 2 | **距離判定の主役は GPS ではなく歩行距離(`CMPedometer`)**。方式2(GPS)は後回しでよい | §14.1、§14.7 |
| 3 | **「止めるまで鳴る」を最優先で作る。** これが満たせなければアプリの存在意義が無い | §7、§9-6 |
| 4 | **背景での検知が成立するかは、実機でしか分からない。** 早い段階で実機スパイクを打つ | §10-1、§10-2 |
| 5 | **コンプリケーションは飾りではなく前提条件。** 無いと背景更新が 1 時間に 1 回まで落ちる | §14.10 |

この結果、**「既存バグの修正 → 鳴り続けるアラーム → 状態機械 → 遡及検出」の順に積む**。
GPS(方式2)とサーバ(Tier 1)は任意の枝として最後に置く。

### 全体構成

```
G0 実機スパイク ──▶ P0 地固め ──▶ P1 鳴り続ける ──▶ P2 状態機械 ──▶ P3 遡及検出 ──▶ G1 検証
                                                                              │
                                                                              ├─▶ P4 iPhone コンパニオン(盗難検知)
                                                                              ├┄▶ P5 GPS 距離 (任意)
                                                                              ├┄▶ P6 サーバ中継 (任意)
                                                                              └┄▶ P7 モーション付きタグ (任意)
```

### 前提条件(満たせないと成立しない)

| 条件 | 理由 |
|---|---|
| **Apple Watch Series 6 以降 + watchOS 9 以降** | 背景 BLE 起床の動作要件(§2.1)。GPS 測位の機種差(§12.3-14)も同じ線で切れる |
| **実機での検証**(シミュレータ不可) | 背景 BLE / WatchConnectivity / CoreMotion はシミュレータで再現しない(§10-1) |
| **文字盤にコンプリケーションを置いてもらう** | 背景更新の予算がこれで決まる(§14.10) |
| iPhone 側は位置情報「常に許可」 | ジオフェンス盗難検知の前提(§15.3) |

---

## G0. 実機スパイク(最優先・使い捨て)

**目的:** 後続の設計を左右する 3 つの未知を、実装前に潰す。

| # | 確かめること | 外れたときの影響 |
|---|---|---|
| G0-1 | 背景 BLE 起床(`bluetooth-central` + notify)が実際に来るか | 来なければ方式1 が丸ごと成立しない。設計の作り直し |
| G0-2 | **5 回/24h の予算が `didDisconnect` 起床も数えるか**(§10-2) | 数えるなら §6 の予算管理を大幅に厳しくする必要がある |
| G0-3 | 背景起床 1 回の実行時間で `queryPedometerData` / `queryActivityStarting` が返るか(§14.4) | 返らなければ P3 の遡及検出が背景で使えず、前面専用に格下げ |

**やり方:** 本体とは別の使い捨てターゲットでよい。ログを `UserDefaults` に貯めて前面で読む。
**完了条件:** 3 項目の実測値が記録され、P2 / P3 の設計に反映されている。

---

## P0. 地固め ── 既存コードの修正(§9)

**目的:** いま動いているものを、正しい土台にする。新機能は足さない。

| ID | 内容 | 対象 | 出典 |
|---|---|---|---|
| P0-1 | `WKBackgroundModes` の無効値 `self-contained` を除去 | `Info.plist` | §9-1 |
| P0-2 | **「切断している」と「離れている」をフラグとして分離**(`isDisconnected` / `isOutOfRange`) | `BLEManager` | §9-2 |
| P0-3 | サービス UUID を定義し `scanForPeripherals(withServices:)` に変更 | `BLEManager` | §9-3 |
| P0-4 | 復帰時の再接続を `retrievePeripherals(withIdentifiers:)` ベースに | `BLEManager` | §9-3 |
| P0-5 | RSSI の移動中央値フィルタ + ヒステリシス(出 2.5m / 復帰 1.5m) | `BLEManager` | §9-5 |
| P0-6 | 1 秒 `Timer` ポーリングを廃し、イベント駆動 + 適応間隔へ | `BLEManager` | §9-4 |
| P0-7 | アラーム状態の永続化(背景終了に耐える) | `AlarmManager` | §9-7 |
| P0-8 | 発火ロジックを View から剥がす下準備(通知経路の整理) | `ContentView` / `AppNotifications` | §9-8 |

**完了条件**
- `performBackgroundRSSICheck()` の未接続分岐が実際に発火する(P0-2 の検証)
- RSSI が境界で振動しない(実機で 2m 付近を往復して確認)
- アプリを背景で終了させ、再起動してもアラーム状態が復元される

---

## P1. 止めるまで鳴るアラーム(§7)

**目的:** README が約束している「手動で停止するまで鳴り続ける」を実際に満たす。
現行実装は**画面が消えた瞬間に鳴り止む**(§9-6)。ここが製品の核。

| ID | 内容 | 対象 | 出典 |
|---|---|---|---|
| P1-1 | `UNNotificationCategory`(`TURETETTE_ALARM`)と「停止」アクションの登録 | `AlarmManager` | §2.4 |
| P1-2 | **通知の連投**(5 秒間隔 × 48 発、`threadIdentifier` で束ねる) | `AlarmManager` | §7 段階2 |
| P1-3 | **全 ID の一括キャンセル**(pending と delivered の両方) | `AlarmManager` | §7 |
| P1-4 | `interruptionLevel = .timeSensitive` + Time Sensitive capability | `AlarmManager` / entitlements | §2.4 |
| P1-5 | `WKExtendedRuntimeSession` でハプティクス継続 + `WKBackgroundModes` に適正値 | `AlarmManager` | §7 段階3 |
| P1-6 | 背景終了 → 前面化でのアラーム継続 | `AlarmManager` | §9-7 |
| P1-7 | 停止導線と状態表示の整理 | `Views/AlarmView` | ― |
| P1-8 | (任意)Critical Alerts entitlement の申請 | ― | §7 段階0 |

**注意**
- `start()` は**前面でしか呼べない**(§2.3)。段階3 は「ユーザが通知をタップしてアプリが前面に来た後」に限る
- セッション種別は用途との整合に審査リスクがある(§10-3)。`physical-therapy` を第一候補に、指摘されたら再検討
- 未配信通知は 64 件上限(§2.4)。48 発は余裕を残した数

**完了条件**
- 画面を消したまま 4 分以上鳴り続ける
- 「停止」を押すと**通知センターに 1 件も残らない**
- 通知タップ → 前面化 → 停止するまで無限にハプティクスが続く

---

## P2. 状態機械 ── 二段トリガの骨格(§6)

**目的:** 判定を 1 か所に集め、誤検知抑制と背景予算管理を入れる。

| ID | 内容 | 対象 | 出典 |
|---|---|---|---|
| P2-1 | **`ProximityCoordinator` 新規**: Idle / Guarding / Suspect / Confirming / Alarming | 新規 | §6 |
| P2-2 | 猶予タイマーと再接続試行(誤検知の吸収) | `ProximityCoordinator` | §3.2、§3.3 |
| P2-3 | 背景予算の監視(`LeGattNear/Exceeded...`)と、枯渇時の縮退 | `BLEManager` | §6 |
| P2-4 | `AppDelegate.handle(_:)` の分岐整理 + `WKWatchConnectivityRefreshBackgroundTask` 追加 | `AppDelegate` | §8-10 |
| P2-5 | `ContentView` を表示専用にする(発火経路を Coordinator に一本化) | `ContentView` | §9-8 |
| P2-6 | しきい値(距離・猶予秒数)の設定画面 | `Views/` | §8-14 |

**完了条件**
- 遮蔽で数秒切れても鳴らない(猶予で吸収される)
- 再接続不能が猶予を超えたら Confirming に入る
- 予算枯渇が UI に出る(「省電力のため検知間隔が長くなっています」)

---

## P3. 遡及検出 ── 歩行距離による判定(§13 / §14)★ 本線

**目的:** 設計上いちばん効く部分。GPS を使わずに「離れた」を判定する。

| ID | 内容 | 対象 | 出典 |
|---|---|---|---|
| P3-1 | `GuardState` の永続化(`lastSeenAt` / `pendingDisconnectAt`)、書き込みは 60 秒間引き | 新規 | §14.2 |
| P3-2 | **`MotionManager` 改修**: `confidence` / `automotive` / `unknown` の状態維持 / debounce / 権限監視 | `MotionManager` | §13.3、§13.14 |
| P3-3 | **`retrospectiveCheck` 決定表**(歩行距離 L・乗り物 V・連続歩行 C・階 F) | `MotionManager` / `ProximityCoordinator` | §14.5 |
| P3-4 | `longestContinuousWalk`(連続歩行時間の算出) | `MotionManager` | §14.6 |
| P3-5 | 監視レベル L0 / L1 / L2 と、レベル依存の猶予・進行中猶予の短縮 | `ProximityCoordinator` | §13.2、§13.8 |
| P3-6 | 保留切断イベント(TTL 30 分)と遅延発報 | `ProximityCoordinator` | §13.8 |
| P3-7 | BAR の適応スケジューリングと再予約 | `AppDelegate` | §13.6、§14.10 |
| P3-8 | **コンプリケーションの提供**と「文字盤に追加してください」のオンボーディング | 新規 `Complications` / `Views` | §14.10 ★ |
| P3-9 | `isDistanceAvailable` / `isFloorCountingAvailable` による機能縮退 | `MotionManager` | §14.8-5 |
| P3-10 | 設定「静止中も鳴らす」 | `Views/` | §13.8 |
| P3-11 | (任意)`HKObserverQuery` + 背景配信で歩数起床 | 新規 `HealthKitManager` | §14.9 |

**判定の核(§14.5)** ── 上から評価し、最初に当たったものを採る。

| # | 条件 | 判定 |
|---|---|---|
| 1 | 歩数データが取れない(権限なし / 未対応) | 判定不能 → 方式1 に従う(= 鳴る) |
| 2 | 乗り物で移動した | 発報(強) |
| 3 | 経路長 ≧ 150m **かつ** 連続歩行 ≧ 60 秒 | 発報(強) |
| 4 | 階移動あり かつ 経路長 ≧ 40m | 発報(中) |
| 5 | 経路長 < 30m | **抑制**(経路長は変位の上界なので確実に近い) |
| 6 | 上記以外 | 弱い通知 1 発のみ |

**完了条件**
- 机に iPhone を置いて席を立つ → 鳴る(遅延発報を含む)
- 同じ部屋の中を 5 分歩き回る → 鳴らない(#5 または #3 の連続歩行条件で抑制)
- Motion & Fitness を拒否した状態でも、見逃しが増えない(#1 が「鳴る」側に倒れる)

---

## G1. 検証ゲート(P3 の後)

| # | 測ること | 判断 |
|---|---|---|
| G1-1 | **BAR の実回数**をコンプリケーション有無で比較(1 日) | §14.10 の前提が正しいかの確認。オンボーディングの強さを決める |
| G1-2 | 遡及問い合わせが背景起床枠の中で返るか | 返らなければ窓を縮めるか前面専用に格下げ |
| G1-3 | 1 日あたりの電池消費 | §10-7。許容できなければ L0 の監視をさらに絞る |
| G1-4 | 誤報・見逃しの実測(1 週間、端末内ログ) | しきい値の調整。ここで初めて P5(GPS)が要るかを判断する |

> **G1-4 の結果で P5 の要否が決まる。** 誤報が許容範囲なら GPS は実装しなくてよい(§14.7)。

---

## P4. iPhone コンパニオン(§3.1 案A / §15)

**目的:** iPhone を守る対象にする。同時に、**Watch 側では原理的に検出できない盗難**を拾う(§15.3)。

| ID | 内容 | 出典 |
|---|---|---|
| P4-1 | iOS ターゲット新規作成。Bundle ID を `WKCompanionAppBundleIdentifier`(現在 `com.turetette`)と一致させる | §8 Tier0-iPhone-5 |
| P4-2 | `CBPeripheralManager`: サービス / characteristic 公開、`bluetooth-peripheral`、状態復元 | §3.1 |
| P4-3 | 低頻度ハートビート notify(Watch の背景起床トリガ。高頻度は予算を焼くので厳禁) | §3.1 |
| P4-4 | 位置 characteristic の read 対応(Watch から能動取得) | §4.1 |
| P4-5 | SLC + `transferUserInfo` で位置を push(`moving` / `src` を同梱) | §4.1、§12.4-D |
| P4-6 | **ジオフェンス盗難検知**: 「置かれた」判定 → 自分の周りに円を登録 → 出たら背景起床 | §15.3 ★ |
| P4-7 | 円の中心・半径・登録時刻の永続化 / `CLMonitor` と `startMonitoring(for:)` の実機比較 | §15.3 |
| P4-8 | 起床後の遡及判定(**歩数 0 + 位置変化 → 盗難**) | §15.4 ★ |
| P4-9 | Watch と BLE 接続中なら誤報を抑制(持ち主が一緒にいる証拠) | §15.3 |
| P4-10 | 「常に許可」のオンボーディングと、拒否時の縮退 UI | §15.3-1 |
| P4-11 | (任意)`CMSensorRecorder` の可用性判定と振動履歴の取得 | §15.4 |

**注意:** `CLMonitor` は iOS 18 以降 `CLServiceSession` が必要で不具合報告も多い一方、
`startMonitoring(for:)` は iOS 27 で deprecated(§15.3-4)。**どちらを採るかは実機比較で決める。**

---

## P5. GPS 距離 ── 方式2(任意・G1-4 の結果次第)(§4 / §12)

**目的:** 「Bluetooth が切れただけ」を黙らせる。**発報には使わない**(§12.4-A)。

| ID | 内容 | 出典 |
|---|---|---|
| P5-1 | `LocationManager` 新規。単発測位と二段タイムアウト(背景 5〜8 秒 / 前面 15〜30 秒) | §12.4-C |
| P5-2 | **3 値判定**(遠い / 近い / 判定不能)と非対称マージン + デッドバンド | §12.4-A、§12.4-B |
| P5-3 | `LastKnownFix` と動的 `maxAge`(iPhone の `moving` フラグで 3 分 / 60 分を切り替え) | §12.4-D |
| P5-4 | 端末内の判定ログ(リングバッファ、外部送信なし) | §12.4-I |
| P5-5 | `NSLocationWhenInUseUsageDescription` 追加、README と `NSMotionUsageDescription` の文言更新 | §12.3-16 |

> **`Bool` で返さないこと。** 判定不能が暗黙に「近い」へ潰れて見逃しになる(§12.4-A)。

---

## P6. サーバ中継 ── Tier 1(任意)(§11)

**目的:** 圏内にいるときだけ効く冗長系。**ローカル完結版を置き換えない。**

| ID | 内容 |
|---|---|
| P6-1 | Watch アプリの独立 APNs 登録(`apns-topic` = watch アプリの bundle ID) |
| P6-2 | `AppDelegate.handle(_:)` に `WKURLSessionRefreshBackgroundTask` 分岐を追加 |
| P6-3 | iPhone: 大幅位置変更で起床 → background `URLSession` でサーバへ位置送信 |
| P6-4 | サーバ: 距離判定と **alert** push の送出(`background` 型は配信保証が無い、§10-11) |
| P6-5 | 二重発報の dedupe(イベント ID)と停止のサーバ同期 |
| P6-6 | 同意 UI、保存期間、削除手段、プライバシーラベル、README 文言 |

> **セルラー限定機能にしないこと。** GPS モデルでも Wi-Fi 圏内なら同じ経路が使える(§11.4)。

---

## P7. モーション付き BLE タグ(任意)(§16.5)

**目的:** 置き忘れと盗難を、距離もしきい値も無しに区別する。

| ID | 内容 |
|---|---|
| P7-1 | タグ側の wake-on-motion 仕様策定(**立ち上がりで 1 回だけ notify**。連投は予算を焼く) |
| P7-2 | タグ × Watch の 2×2 判定(タグ静止 × 自分移動 = 置き忘れ / タグ移動 × 自分静止 = 盗難) |

> **AirTag は使えない**(識別子が約 15 分で回転し、追跡対象にできない。§16.2)。

---

## 1. 参照の対応表

| 設計文書のタスク表 | この計画での位置 |
|---|---|
| §8 Tier 0 / Watch(1〜14) | P0、P1、P2 |
| §13.13(15〜22) | P3 |
| §14.11(23〜29) | P3 |
| §8 Tier 0 / iPhone(1〜5) | P4 |
| §15.6(6〜12) | P4 |
| §8 Tier 1(1〜6) | P6 |
| §9(1〜8) | P0 |
| §10(未検証事項) | G0、G1 |

---

## 2. 進捗の更新方法

[docs/roadmap.html](roadmap.html) の先頭にある `PLAN` 配列で状態を管理している。

1. 該当タスクの `s`(status)を書き換える ── `"todo"` / `"wip"` / `"done"` / `"skip"`
2. `CURRENT` を現在着手中のフェーズ ID に合わせる
3. `UPDATED` を更新日に書き換える

進捗率・現在地マーカー・フェーズの状態チップはすべてここから自動で計算される。
